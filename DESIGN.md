# DESIGN.md — 식사 룰렛

AI 에이전트와 사람이 UI를 만들거나 고칠 때 읽는 디자인 시스템 문서다.
기준은 스펙 [15-visual-design.md](docs/superpowers/specs/2026-09-04-lunch-roulette/15-visual-design.md)이며, 이 문서와 스펙이 충돌하면 스펙이 우선한다.
값은 `src/app/globals.css`의 `:root` 토큰과 클래스에서 그대로 가져왔다. 새 값을 만들지 말고 여기 있는 것만 쓴다.

## 1. 한 줄 정체성

**동네 백반집의 흰 메뉴판과 도장 쿠폰.** 흰 바탕, 검은 선, 조용한 본문. 대담함은 빨간 도장 한 곳에만 쓴다.

레벨 이름(처음, 익숙, 단골, 찐단골, 집밥)이 이 앱의 정체성이다. 화면의 모든 요소는 이 어휘에서 나온다.

| 화면 요소 | 비유 | 클래스 |
|---|---|---|
| 룰렛 영역 | 벽에 걸린 흰 메뉴판 (검은 테두리 2겹) | `.board` |
| 후보 3개 | 식권 3장 (본문 + 점선 절취선 + 도장 칸) | `.ticket` |
| 확정 | 인주 도장 (빨간 원형, 기울어짐) | `.seal` |
| 레벨 | 도장 5칸 (찍힌 칸 수가 레벨) | `.stamps` |
| 기록 목록 | 뜯어낸 식권 반쪽 더미 (점선 괘선) | `.stub` |
| 지도 위 마커 | 인주 도장 (숫자는 방문 횟수, 크기는 레벨) | `.stamp-marker` |
| 내 정보 | 회원증 카드 (식권 어휘 재사용) | `.ticket` |

## 2. 색

토큰은 6개뿐이다. 이 밖의 색은 쓰지 않는다. 단일 라이트 테마이며 다크 모드는 없다(`color-scheme: light`).

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--paper` | `#FFFFFF` | 바탕, 카드, 지도 위 상자 |
| `--ink` | `#000000` | 글자, 테두리, 괘선, 장소 마커 점 |
| `--seal` | `#D62E2E` | 돌리기 버튼, 확정 도장, 도장 5칸, 도장 마커, 오류 문구, 포커스 링, 현재 탭 밑줄 |
| `--seal-deep` | `#B3201F` | 누른 상태의 돌리기 버튼, 레벨 4~5 마커 테두리, 주의 문구 |
| `--muted` | `#6F6F6F` | 주소, 날짜, 보조 문구, 비활성 탭 |
| `--rule` | `#E3E3E3` | 약한 구분선, 지도 로딩 바탕 |

규칙:

- 빨간색(`--seal`)은 "찍힌 것"과 "누르면 결과가 생기는 단 하나의 버튼"에만 쓴다. 화면당 빨간 버튼은 홈의 돌리기 하나뿐이다.
- 회색은 `--muted`와 `--rule` 둘만 있다. 중간 회색을 새로 만들지 않는다.
- 비활성은 색을 바꾸지 않고 `opacity: 0.35`(버튼) 또는 `0.5`(입력, 선택)로 표현한다.

## 3. 서체

둘 다 Google Fonts를 `next/font/google`로 자체 호스팅한다(`src/app/layout.tsx`). 외부 폰트 링크나 다른 서체를 추가하지 않는다.

| 역할 | 서체 | 굵기 | 변수 |
|---|---|---|---|
| 표시 | Black Han Sans | 400 | `--font-display` |
| 본문 | IBM Plex Sans KR | 400 / 500 / 700 | `--font-body` |

표시 서체는 식당 이름, 메뉴판 글자, 화면 제목, 장소 선택, 로그인 제목, 도장 마커 숫자, 확정 도장 글자에만 쓴다. 그 밖은 전부 본문 서체다.

크기 단계:

| 토큰 | 값 | 예 |
|---|---|---|
| `--text-sm` | 13px | 주소, 날짜, 보조 문구, 관리자 버튼 |
| `--text-md` | 16px | 본문 기본 |
| `--text-lg` | 20px | 식권 이름, 절 제목(700), 돌리기 버튼, 슬롯 라벨 |
| `--text-xl` | 25px | 화면 제목(표시 서체) |
| `--text-2xl` | 32px | 메뉴판 이름, 장소 선택(표시 서체) |
| `--text-3xl` | 40px | 확정 결과 이름, 로그인 제목(표시 서체) |

줄 간격: 본문 1.5, 표시 1.1~1.2. 한글은 `word-break: keep-all`. 대문자 눈썹 라벨, 자간 벌리기는 쓰지 않는다.

## 4. 레이아웃과 간격

- 모바일 세로 한 열. `.page`는 최대 너비 480px, 좌우 여백 20px, 위 24px, 아래는 하단 탭 높이(`--tabbar-h` 56px) + 32px.
- 본문은 왼쪽 정렬. 메뉴판 안 글자와 로그인 카드만 가운데 정렬.
- 간격은 4의 배수만 쓴다. 자주 쓰는 값: 4(행 안 줄 간격), 8(도장 칸, 버튼 사이), 12(카드 사이, 절 안), 14(목록 항목 상하, 식권 안), 20(좌우 여백), 24(절 사이).
- 모서리 반경은 4px 하나다(버튼, 식권, 입력). 원형(50%)은 도장, 마커, 사진에만.
- 테두리 굵기: 1px(약한 괘선 `--rule`), 1.5px(카드·버튼·입력 `--ink`), 3px(메뉴판·확정 결과 바깥 테두리, 도장, 포커스 링). 메뉴판의 안쪽 선은 6px 안에 1px.
- 그림자는 쓰지 않는다. 지도 위 요소만 `box-shadow`로 흰 띠 + 검은 고리를 그려 "테두리 2겹"을 만든다(`.map-box`). 이것은 그림자가 아니라 테두리다.
- 기록 화면은 유일한 예외로 지도가 `position: fixed`로 하단 탭 위까지 전체를 채운다(`.map-screen`). 그 위 요소(플로팅 버튼, 식권 카드, 안내 상자)는 `.map-box` 어휘를 쓴다.

## 5. 컴포넌트 어휘

새 컴포넌트가 필요하면 먼저 아래에서 조합한다. 인라인 스타일과 새 색상 값은 쓰지 않는다.

| 목적 | 클래스 | 규칙 |
|---|---|---|
| 기본 버튼 | `.btn` | 흰 바탕, ink 1.5px 테두리, 높이 48px, 굵기 600 |
| 주 버튼 | `.btn.btn-primary` | seal 바탕, 높이 56px, 20px. 화면에 하나뿐(홈 돌리기, 확정 확인) |
| 폭 채우기 | `.btn-block` | |
| 텍스트 버튼 | `.btn-text` | muted 밑줄. 다시 돌리기, 취소, 뒤로 |
| 관리자 버튼 | `.btn-admin` | seal 점선 테두리, 36px, 13px. 관리자에게만 |
| 입력 | `.input`, `.field`, `.form` | ink 1.5px, 안쪽 12px, 라벨 500 |
| 메뉴판 | `.board`, `.board-name`, `.board-caption` | 테두리 2겹, 가운데 정렬 |
| 식권 | `.ticket`, `.ticket-main`, `.ticket-name`, `.ticket-address`, `.ticket-stub` | 왼쪽 본문, 오른쪽 점선 절취선 안에 `LevelStamps compact`. 선택은 `aria-pressed` + seal 안쪽 띠 |
| 확정 결과 | `.result`, `.result-name`, `.seal` | 테두리 3px, 오른쪽 위 도장 84px, -12° |
| 도장 5칸 | `LevelStamps` 컴포넌트 (`.stamps`) | 12px 원, seal 테두리, 찍힌 칸은 채움. 직접 마크업하지 말고 컴포넌트를 쓴다 |
| 목록(실선) | `.rows`, `.row`, `.row-title`, `.row-meta` | 장소 목록. 첫 항목 위 ink 1.5px, 사이 `--rule` 1px |
| 목록(점선) | `.stubs`, `.stub`, `.stub-name`, `.stub-meta` | 기록 목록. 사이 ink 점선 |
| 하단 탭 | `.tabbar` | 홈·장소·기록·내 정보. 현재 탭은 `aria-current="page"` + seal 밑줄 3px |
| 지도 위 상자 | `.map-box` + `.map-fab` / `.map-ticket` / `.map-notice` / `.map-fail` | 흰 바탕, 테두리 2겹, 그림자 없음 |
| 도장 마커 | `.stamp-marker.l1`~`.l5`, `.selected` | 지름 32·36·40·44·48px, 레벨 4부터 seal-deep 3px 테두리 |
| 장소 마커 | `.place-marker` | ink 점 10px + 흰 라벨. 누를 수 없음 |
| 보조 문구 | `.muted`, `.small`, `.alert`(seal 500), `.note`(seal-deep 13px) | |

## 6. 움직임

재생하는 움직임은 두 개뿐이다.

1. 돌리기 후 메뉴판 이름이 80ms마다 바뀌다 1.5~2초 뒤 멈춘다. 재생 중 모든 버튼을 잠근다.
2. 확정 직후 도장이 찍힌다(`.seal.is-stamping`, 0.32s, 크게 나타나 제자리로).

`prefers-reduced-motion: reduce`면 둘 다 재생하지 않고 결과만 보여 준다. 카드 호버, 화면 진입 페이드, 스켈레톤, 스피너, 지도 마커·카드·오버레이 전환 효과는 넣지 않는다.

## 7. 접근성

- 포커스 링은 `--seal` 3px, 바깥 2px(지도 위 요소는 4~6px). 지우지 않는다.
- 도장 5칸은 `role="img"`에 "레벨 N 이름, 다음 레벨까지 N회". 5레벨은 뒤 문구를 뺀다.
- 돌리는 중 메뉴판은 `role="status"`로 "후보를 뽑는 중입니다", 바뀌는 이름은 `aria-hidden`.
- 후보 식권은 `aria-pressed`, 하단 탭 현재 화면은 `aria-current="page"`.
- 지도 화면은 키보드 대체 경로가 목록 오버레이다. 플로팅 버튼 → 목록 → 항목 → 닫기가 키보드만으로 되어야 한다.
- 버튼 최소 높이 48px(텍스트 버튼 제외). 탭 타깃은 44px 이상.

## 8. 문구

문구는 스펙 [07-screens.md](docs/superpowers/specs/2026-09-04-lunch-roulette/07-screens.md), [08-errors.md](docs/superpowers/specs/2026-09-04-lunch-roulette/08-errors.md), 15, 16, 18의 문구 표를 그대로 쓴다. 새 문구는 스펙에 먼저 적고 코드에 넣는다.

어투 규칙:

- 존댓말 평서문. "~합니다", "~입니다". 느낌표는 "레벨 업!" 하나뿐.
- 짧게. 한 상자에 한 문장.
- 날짜는 "2026년 9월 4일 점심, 회사"처럼 쉼표로 잇는다. 가운뎃점(·)은 쓰지 않는다.
- 영어 문구, 이모지, 아이콘 폰트를 쓰지 않는다. 고유명사(Google, 카카오)만 예외다. 아이콘은 플로팅 버튼의 SVG(목록 ≡, 닫기 ×)와 장소 선택의 ▾뿐이다.

## 9. 하지 않는 것

에이전트가 습관적으로 넣기 쉬운 것들이다. 아래가 diff에 보이면 되돌린다.

- 크림색·베이지 바탕, 테라코타·주황 강조, 파스텔, 그라데이션
- 둥근 카드(반경 8px 이상), 카드 그림자, 유리 효과
- 눈썹 대문자 라벨, 넓은 자간, 얇은 굵기(300 이하)
- 새 서체, 아이콘 라이브러리, 이모지
- 두 번째 빨간 버튼, 초록·파랑·노랑 상태 색
- 호버 확대, 페이드 인, 스켈레톤, 스피너, 토스트
- 다크 모드, 테마 전환
- 인라인 `style`, Tailwind, CSS-in-JS. 스타일은 `globals.css` 한 곳에만 둔다
- 가운뎃점(·), 말줄임 "…" 남용, 영어 문구

## 10. 새 화면을 만들 때 순서

1. 스펙 07에서 화면과 상태를 찾는다. 없으면 스펙부터 쓴다(`docs/superpowers/specs/.../README.md` 인덱스에 추가).
2. 위 5절의 어휘로 화면을 조립한다. 부족한 클래스는 `globals.css`에 토큰만 써서 추가하고, 이 문서 5절에 한 줄 더한다.
3. 상태별 표시(빈 상태, 오류, 로딩, 비활성)를 스펙 08 문구로 채운다. 로딩은 문구 한 줄이지 스피너가 아니다.
4. 390px 폭에서 확인한다. 최대 480px까지 늘어나도 한 열이다.
5. 키보드로 끝까지 조작해 본다. 포커스 링이 보여야 한다.
6. `prefers-reduced-motion`에서 움직임이 꺼지는지 본다.
