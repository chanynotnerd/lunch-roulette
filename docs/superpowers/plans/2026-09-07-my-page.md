# 내 정보(마이페이지) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 하단 탭 4번째 화면 "내 정보"(`/me`)에 Google 계정의 사진·이름·이메일과 로그아웃 버튼을 두고, 탭바의 로그아웃 버튼을 없앤다.

**Architecture:** Supabase Auth가 auth.users에 저장한 Google `user_metadata`를 서버에서 읽어 `Profile` 타입으로 가공한다(순수 함수 `toProfile` + 읽기 함수 `loadProfile`). 화면은 `(app)` 그룹의 서버 컴포넌트이고, 사진 로드 실패 대체만 작은 클라이언트 컴포넌트 `Avatar`가 맡는다. DB, 서버 액션, 룰렛 규칙은 바꾸지 않는다.

**Tech Stack:** Next.js 16(App Router, 서버 컴포넌트, 서버 액션), React 19, `@supabase/ssr`, Vitest, 전역 CSS(`src/app/globals.css`, 스펙 15 토큰).

**Spec:** `docs/superpowers/specs/2026-09-04-lunch-roulette/18-my-page.md` (07, 15, 13, README는 이미 갱신됨. 커밋 48d7673)

## Global Constraints

- 스펙 18 P3: profiles 테이블·마이그레이션 없음. `user_metadata`의 `full_name`, `name`, `avatar_url`, `picture`, `email` 다섯 키만 본다.
- 스펙 18 P4: 사진은 `next/image`가 아닌 일반 `<img>` + `referrerPolicy="no-referrer"`.
- 스펙 18 P5: 로그아웃은 확인 단계 없음. 기존 서버 액션 `signOut`(`src/actions/auth.ts`) 그대로 사용.
- 스펙 18 P6: 관리자 "오늘 기록 초기화" 버튼은 홈·기록에 그대로 둔다. 내 정보에 넣지 않는다.
- 스펙 15: 빨간 버튼(`.btn-primary`)은 홈의 돌리기 하나뿐. 로그아웃은 `.btn.btn-block`. 카드 그림자 금지. 화면 진입 페이드 금지.
- 스펙 15 레이아웃: 한 열, 최대 480px, 좌우 여백 20px(`.page`가 이미 적용).
- 문구는 스펙 18 "문구" 절 그대로: "내 정보", "Google 계정으로 로그인되어 있습니다", "로그아웃", 이름 대체 "사용자".
- `server-only`를 import하는 모듈의 테스트는 `vi.mock('server-only', () => ({}))`를 먼저 선언한다(`tests/actions/admin.test.ts` 관례).
- 기존 `requireUser`(`src/lib/auth.ts`)와 그 호출부는 손대지 않는다.
- 커밋 메시지는 한국어 요약 + 꼬리말(아래 형식). 커밋마다 `npm test`가 통과해야 한다.
- 이 저장소는 다른 세션이 스펙 17(식당 수집 확장)을 동시에 작업 중이다. `src/places/*`, `src/actions/places.ts`, `docs/superpowers/plans/2026-09-07-restaurant-search-expansion.md`, `scripts/spike-kakao-search.ts`는 건드리지 않고, 커밋할 때는 이 계획의 파일만 `git add`한다(`git add -A` 금지).

커밋 꼬리말(모든 커밋 공통):

```
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013iuoahSnk53afoXpqw9aMn
```

## 파일 구조

| 파일 | 역할 | 작업 |
|---|---|---|
| `src/lib/profile.ts` | `Profile` 타입, 순수 함수 `toProfile`, 읽기 함수 `loadProfile`. `server-only` | 생성 (Task 1) |
| `tests/lib/profile.test.ts` | `toProfile` 분기 5개 | 생성 (Task 1) |
| `src/app/(app)/me/Avatar.tsx` | 원형 사진. 로드 실패나 URL 없음이면 이니셜 원. 클라이언트 컴포넌트 | 생성 (Task 2) |
| `src/app/globals.css` | `.member-card`, `.avatar` 스타일 추가. `.tabbar` 4칸, `.tabbar form`/`.tabbar-signout` 삭제 | 수정 (Task 2, 3) |
| `src/app/(app)/me/page.tsx` | S5 화면. `loadProfile` → 회원증 카드 + 안내 + 로그아웃 폼 | 생성 (Task 3) |
| `src/app/components/Nav.tsx` | 탭 4개. `signOut` 폼 제거 | 수정 (Task 3) |
| `docs/superpowers/handoff/2026-09-04-session-handoff.md` | 구현·확인 결과 한 줄 | 수정 (Task 4) |

---

### Task 1: `toProfile` 순수 함수와 `loadProfile`

**Files:**
- Create: `src/lib/profile.ts`
- Test: `tests/lib/profile.test.ts`

**Interfaces:**
- Consumes: `createClient` from `@/lib/supabase/server` (기존. `Promise<SupabaseClient>`; `auth.getUser()` 사용)
- Produces:
  ```ts
  export type Profile = { name: string; email: string | null; avatarUrl: string | null; initial: string }
  export type ProfileSource = { email?: string | null; user_metadata?: Record<string, unknown> | null }
  export function toProfile(user: ProfileSource): Profile
  export async function loadProfile(): Promise<Profile | null>
  ```

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/lib/profile.test.ts`:

```ts
import { describe, expect, it, vi } from 'vitest'

// server-only 는 RSC 밖에서 import 하면 던진다. 순수 함수만 검사하므로 빈 모듈로 바꾼다.
vi.mock('server-only', () => ({}))
// loadProfile이 import하는 서버 클라이언트는 next/headers에 기대므로 테스트에서는 비워 둔다.
vi.mock('@/lib/supabase/server', () => ({ createClient: vi.fn() }))

import { toProfile } from '@/lib/profile'

describe('toProfile', () => {
  it('full_name과 avatar_url이 있으면 그대로 쓴다', () => {
    const p = toProfile({
      email: 'chanyoung@gmail.com',
      user_metadata: {
        full_name: '김찬영',
        name: '찬영',
        avatar_url: 'https://lh3.googleusercontent.com/a/full',
        picture: 'https://lh3.googleusercontent.com/a/pic',
      },
    })
    expect(p).toEqual({
      name: '김찬영',
      email: 'chanyoung@gmail.com',
      avatarUrl: 'https://lh3.googleusercontent.com/a/full',
      initial: '김',
    })
  })

  it('full_name이 없으면 name을, avatar_url이 없으면 picture를 쓴다', () => {
    const p = toProfile({
      email: 'a@b.com',
      user_metadata: { name: '찬영', picture: 'https://lh3.googleusercontent.com/a/pic' },
    })
    expect(p.name).toBe('찬영')
    expect(p.avatarUrl).toBe('https://lh3.googleusercontent.com/a/pic')
    expect(p.initial).toBe('찬')
  })

  it('이름 키가 없으면 이메일의 @ 앞부분을 이름으로 쓴다', () => {
    const p = toProfile({ email: 'boltwriter721@gmail.com', user_metadata: {} })
    expect(p.name).toBe('boltwriter721')
    expect(p.initial).toBe('b')
    expect(p.avatarUrl).toBeNull()
  })

  it('이름도 이메일도 없으면 "사용자"이고 사진은 null이다', () => {
    const p = toProfile({ email: null, user_metadata: null })
    expect(p).toEqual({ name: '사용자', email: null, avatarUrl: null, initial: '사' })
  })

  it('공백만 있는 이름과 빈 문자열 사진은 없는 것으로 본다', () => {
    const p = toProfile({
      email: 'x@y.com',
      user_metadata: { full_name: '   ', name: '', avatar_url: '', picture: '  ' },
    })
    expect(p.name).toBe('x')
    expect(p.avatarUrl).toBeNull()
  })

  it('user_metadata의 email이 있고 최상위 email이 없으면 그것을 쓴다', () => {
    const p = toProfile({ user_metadata: { email: 'meta@y.com' } })
    expect(p.email).toBe('meta@y.com')
    expect(p.name).toBe('meta')
  })
})
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npx vitest run tests/lib/profile.test.ts`
Expected: FAIL. `Failed to resolve import "@/lib/profile"` 또는 유사한 모듈 없음 오류.

- [ ] **Step 3: 최소 구현**

`src/lib/profile.ts`:

```ts
import 'server-only'
import { createClient } from '@/lib/supabase/server'

/** 내 정보 화면이 쓰는 프로필. 스펙 18 "데이터". */
export type Profile = {
  /** full_name → name → 이메일 @ 앞 → "사용자" */
  name: string
  email: string | null
  /** avatar_url → picture → null */
  avatarUrl: string | null
  /** 사진이 없을 때 원 안에 넣는 이름 첫 글자 */
  initial: string
}

/** Supabase User 중 프로필에 필요한 부분만. 테스트에서 통째로 만들기 쉽도록 좁혀 둔다. */
export type ProfileSource = {
  email?: string | null
  user_metadata?: Record<string, unknown> | null
}

const FALLBACK_NAME = '사용자'

/** 문자열이고 공백을 뗀 뒤 비어 있지 않으면 그 값, 아니면 null. */
function nonEmptyString(value: unknown): string | null {
  if (typeof value !== 'string') return null
  const trimmed = value.trim()
  return trimmed.length > 0 ? trimmed : null
}

/** Google 메타데이터에서 Profile을 만든다. 순수 함수. */
export function toProfile(user: ProfileSource): Profile {
  const meta = user.user_metadata ?? {}
  const email = nonEmptyString(user.email) ?? nonEmptyString(meta.email)
  const name =
    nonEmptyString(meta.full_name) ??
    nonEmptyString(meta.name) ??
    (email ? email.split('@')[0] : null) ??
    FALLBACK_NAME
  const avatarUrl = nonEmptyString(meta.avatar_url) ?? nonEmptyString(meta.picture)
  return { name, email, avatarUrl, initial: name.charAt(0) }
}

/** 현재 요청의 프로필. 비로그인이면 null. 화면 전용이며 서버 액션은 requireUser를 쓴다. */
export async function loadProfile(): Promise<Profile | null> {
  const supabase = await createClient()
  const {
    data: { user },
    error,
  } = await supabase.auth.getUser()
  if (error || !user) return null
  return toProfile(user)
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run tests/lib/profile.test.ts`
Expected: PASS, 6 tests.

Run: `npm test`
Expected: 전체 통과(기존 테스트 수 + 6).

- [ ] **Step 5: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 오류 없음. (`toProfile(user)`에 Supabase `User`를 넘길 수 있는지 확인. `User.user_metadata`는 `UserMetadata`(=`{ [key: string]: any }`)라 `Record<string, unknown>`에 대입된다.)

- [ ] **Step 6: 커밋**

```bash
git add src/lib/profile.ts tests/lib/profile.test.ts
git commit -m "feat(profile): Google 메타데이터에서 Profile을 만드는 toProfile과 loadProfile (스펙 18)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013iuoahSnk53afoXpqw9aMn"
```

---

### Task 2: `Avatar` 클라이언트 컴포넌트와 회원증 카드 CSS

**Files:**
- Create: `src/app/(app)/me/Avatar.tsx`
- Modify: `src/app/globals.css` (`.tabbar-signout` 블록 바로 뒤, `/* 홈 상단 */` 주석 앞에 새 블록 추가)

**Interfaces:**
- Consumes: 없음 (Task 1의 `Profile`에서 `avatarUrl`, `initial`, `name`을 받아 쓰지만 타입 import는 하지 않는다. `server-only` 모듈을 클라이언트에서 import하면 빌드가 깨진다.)
- Produces:
  ```tsx
  export default function Avatar(props: { src: string | null; initial: string; name: string }): JSX.Element
  ```
  CSS 클래스: `.member-card`, `.member-card-main`, `.member-card-name`, `.member-card-email`, `.avatar`, `.avatar-img`, `.avatar-initial`

- [ ] **Step 1: `Avatar` 작성**

`src/app/(app)/me/Avatar.tsx`:

```tsx
'use client'

import { useState } from 'react'

type Props = {
  src: string | null
  /** 사진이 없거나 로드에 실패했을 때 원 안에 넣는 글자 */
  initial: string
  /** img alt와 이니셜 원의 aria-label */
  name: string
}

/**
 * 지름 56px 원형 프로필 사진. 스펙 18 P4: next/image 대신 img + no-referrer.
 * 로드 실패(onError)는 서버 컴포넌트에서 잡을 수 없어 이 컴포넌트만 클라이언트다.
 */
export default function Avatar({ src, initial, name }: Props) {
  const [failed, setFailed] = useState(false)
  const showImage = src !== null && !failed

  if (!showImage) {
    return (
      <span className="avatar avatar-initial" role="img" aria-label={`${name} 프로필`}>
        {initial}
      </span>
    )
  }

  return (
    // eslint-disable-next-line @next/next/no-img-element -- 스펙 18 P4. Google 사진 도메인을 next.config에 등록하지 않는다.
    <img
      className="avatar avatar-img"
      src={src}
      alt={`${name} 프로필`}
      width={56}
      height={56}
      referrerPolicy="no-referrer"
      onError={() => setFailed(true)}
    />
  )
}
```

- [ ] **Step 2: CSS 추가**

`src/app/globals.css`에서 `.tabbar-signout { ... }` 블록 뒤, `/* 홈 상단: 장소 선택 + 슬롯 */` 주석 앞에 넣는다. (`.tabbar-signout` 블록 자체는 Task 3에서 지운다. 이 단계에서는 위치만 잡는다.)

```css
/* 내 정보: 회원증 카드. 스펙 18. 식권(.ticket)과 같은 어휘, 누를 수 없다 */
.member-card {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 16px;
  align-items: center;
  padding: 16px;
  border: 1.5px solid var(--ink);
  border-radius: 4px;
  background: var(--paper);
}

.member-card-main {
  display: grid;
  gap: 4px;
  min-width: 0;
}

.member-card-name {
  font-family: var(--font-display);
  font-size: var(--text-lg);
  line-height: 1.2;
  overflow-wrap: anywhere;
}

.member-card-email {
  color: var(--muted);
  font-size: var(--text-sm);
  overflow-wrap: anywhere;
}

.avatar {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  flex: none;
}

.avatar-img {
  object-fit: cover;
  display: block;
}

.avatar-initial {
  display: grid;
  place-items: center;
  border: 1.5px solid var(--ink);
  background: var(--paper);
  color: var(--ink);
  font-family: var(--font-display);
  font-size: var(--text-xl);
  line-height: 1;
}
```

- [ ] **Step 3: 린트와 타입 검사**

Run: `npx eslint "src/app/(app)/me/Avatar.tsx"`
Expected: 오류·경고 없음. (`no-img-element`가 disable 주석으로 잡히지 않으면 규칙 이름을 `npx eslint --print-config "src/app/(app)/me/Avatar.tsx" | grep img`로 확인해 고친다.)

Run: `npx tsc --noEmit`
Expected: 오류 없음.

- [ ] **Step 4: 커밋**

```bash
git add "src/app/(app)/me/Avatar.tsx" src/app/globals.css
git commit -m "feat(profile): 원형 프로필 사진 Avatar와 회원증 카드 스타일 (스펙 18)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013iuoahSnk53afoXpqw9aMn"
```

---

### Task 3: `/me` 화면과 하단 탭 4칸

**Files:**
- Create: `src/app/(app)/me/page.tsx`
- Modify: `src/app/components/Nav.tsx` (전체 교체)
- Modify: `src/app/globals.css` (`.tabbar` `grid-template-columns`, `.tabbar form`과 `.tabbar-signout` 블록 삭제)

**Interfaces:**
- Consumes: `loadProfile(): Promise<Profile | null>` from `@/lib/profile` (Task 1), `Avatar` from `./Avatar` (Task 2), `signOut` from `@/actions/auth` (기존 서버 액션, 인자 없음, 로그아웃 후 `/login`으로 redirect)
- Produces: 경로 `/me`. 탭바 링크 `{ href: '/me', label: '내 정보' }`.

- [ ] **Step 1: 페이지 작성**

`src/app/(app)/me/page.tsx`:

```tsx
import { redirect } from 'next/navigation'
import { signOut } from '@/actions/auth'
import { loadProfile } from '@/lib/profile'
import Avatar from './Avatar'

export const dynamic = 'force-dynamic'

/** S5. 내 정보: Google 계정 사진·이름·이메일과 로그아웃. 스펙 18. */
export default async function MePage() {
  const profile = await loadProfile()
  // proxy가 먼저 막지만, 세션이 그 사이 만료됐을 때를 위해 한 번 더 보낸다.
  if (!profile) redirect('/login')

  return (
    <>
      <h1 className="page-title">내 정보</h1>

      <div className="member-card">
        <Avatar src={profile.avatarUrl} initial={profile.initial} name={profile.name} />
        <div className="member-card-main">
          <div className="member-card-name">{profile.name}</div>
          {profile.email && <div className="member-card-email">{profile.email}</div>}
        </div>
      </div>

      <p className="muted small">Google 계정으로 로그인되어 있습니다</p>

      <form action={signOut}>
        <button type="submit" className="btn btn-block">
          로그아웃
        </button>
      </form>
    </>
  )
}
```

- [ ] **Step 2: `Nav.tsx` 교체**

`src/app/components/Nav.tsx` 전체:

```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'

const TABS = [
  { href: '/', label: '홈' },
  { href: '/places', label: '장소' },
  { href: '/records', label: '기록' },
  { href: '/me', label: '내 정보' },
] as const

/** 하단 탭: 홈, 장소, 기록, 내 정보. 현재 화면은 aria-current로 표시한다. 로그아웃은 내 정보 안에 있다. 스펙 07 공통, 15 접근성, 18. */
export default function Nav() {
  const pathname = usePathname()
  const isCurrent = (href: string) => (href === '/' ? pathname === '/' : pathname.startsWith(href))

  return (
    <nav className="tabbar" aria-label="주요 화면">
      {TABS.map((t) => (
        <Link key={t.href} href={t.href} aria-current={isCurrent(t.href) ? 'page' : undefined}>
          {t.label}
        </Link>
      ))}
    </nav>
  )
}
```

- [ ] **Step 3: 탭바 CSS 수정**

`src/app/globals.css`의 `.tabbar` 블록에서 한 줄을 바꾼다.

```css
  grid-template-columns: 1fr 1fr 1fr auto;
```
→
```css
  grid-template-columns: repeat(4, 1fr);
```

그리고 아래 두 블록을 통째로 지운다.

```css
.tabbar form {
  display: grid;
}

.tabbar-signout {
  background: none;
  border: none;
  padding: 0 16px;
  color: var(--muted);
  font-size: var(--text-sm);
  cursor: pointer;
}
```

확인: `grep -n "tabbar-signout\|signOut" src/app/globals.css src/app/components/Nav.tsx` 결과가 비어야 한다.

- [ ] **Step 4: 빌드·린트·테스트**

Run: `npm run lint`
Expected: 오류 없음.

Run: `npm test`
Expected: 전체 통과.

Run: `npm run build`
Expected: 성공. 출력의 라우트 목록에 `/me`가 `ƒ (Dynamic)`으로 보인다. `server-only` 모듈을 클라이언트에서 import했다는 오류가 없어야 한다.

- [ ] **Step 5: 로컬에서 눈으로 확인**

`.env.local`이 있는 상태에서 `npm run dev`를 띄우고 http://localhost:3000 에 Google 계정으로 로그인한 뒤:

1. 하단 탭이 "홈·장소·기록·내 정보" 4칸 균등이고 로그아웃 글자는 없다.
2. "내 정보" 탭을 누르면 `/me`. 탭에 빨간 밑줄. 제목 "내 정보", 회원증 카드에 Google 사진(원형)·이름·이메일, 그 아래 "Google 계정으로 로그인되어 있습니다", 전체 폭 흰 "로그아웃" 버튼.
3. 브라우저 개발자 도구로 사진 `<img>`의 `src`를 존재하지 않는 URL로 바꾸면 이니셜 원으로 바뀐다(onError 경로).
4. "로그아웃"을 누르면 `/login`으로 간다. 다시 `/me`를 주소창에 치면 `/login`으로 튕긴다.
5. 홈과 기록 화면의 관리자 버튼(ADMIN_EMAILS 계정)이 그대로 있다.

문제가 있으면 여기서 고치고 Step 4를 다시 돈다.

- [ ] **Step 6: 커밋**

```bash
git add "src/app/(app)/me/page.tsx" src/app/components/Nav.tsx src/app/globals.css
git commit -m "feat(profile): 내 정보 화면(/me)과 하단 탭 4칸, 탭바 로그아웃 제거 (스펙 18)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013iuoahSnk53afoXpqw9aMn"
```

---

### Task 4: 배포와 인수인계 갱신

**Files:**
- Modify: `docs/superpowers/handoff/2026-09-04-session-handoff.md` ("문서 위치" 절과 "다음 할 일" 절)

**Interfaces:**
- Consumes: Task 3까지의 커밋. Vercel 프로젝트 lunch-roulette(Git 연동됨).
- Produces: 프로덕션 https://lunch-roulette-sooty.vercel.app 에서 `/me` 동작.

- [ ] **Step 1: 배포**

이 저장소는 Vercel Git 연동이 되어 있어 `main`에 push하면 프로덕션이 배포된다. 다른 세션의 미커밋 변경이 함께 올라가지 않도록 push 전에 `git status --short`로 이 계획의 파일이 모두 커밋됐는지 확인한다.

Run: `git push origin main`
Expected: 성공. 이어서 `npx vercel ls lunch-roulette --prod 2>/dev/null | head -5` 또는 Vercel 대시보드에서 새 배포가 Ready가 될 때까지 기다린다(보통 2~3분).

Git 연동 배포가 뜨지 않으면 `npx vercel --prod --yes`로 직접 배포한다.

- [ ] **Step 2: 프로덕션 확인**

https://lunch-roulette-sooty.vercel.app 에 로그인해 Task 3 Step 5의 1, 2, 4를 다시 확인한다. 특히 사진이 보이는지(Google 이미지가 no-referrer로 로드되는지) 확인한다.

- [ ] **Step 3: 인수인계 문서 갱신**

`docs/superpowers/handoff/2026-09-04-session-handoff.md`:

"문서 위치" 절의 스펙 줄에서 `01~16` 부분을 아래처럼 바꾼다(이미 17이 반영돼 있으면 18만 덧붙인다).

```
- 스펙(source of truth): `docs/superpowers/specs/2026-09-04-lunch-roulette/` 01~18. 13은 백로그, 14는 유저플로우 해피케이스(Mermaid 원본), 15는 시각 디자인, 16은 퍼스널 맵(기록 탭 지도), 17은 식당 수집 확장, 18은 내 정보(마이페이지).
```

"다음 할 일" 절 끝에 한 줄을 더한다.

```
10. ~~내 정보 화면(스펙 18)~~ 2026-09-07 구현·배포 완료. 계획 `docs/superpowers/plans/2026-09-07-my-page.md`. 통계·탈퇴는 백로그 B10.
```

- [ ] **Step 4: 커밋**

```bash
git add docs/superpowers/handoff/2026-09-04-session-handoff.md
git commit -m "docs: 인수인계에 내 정보 화면(스펙 18) 구현·배포 결과 기록

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013iuoahSnk53afoXpqw9aMn"
git push origin main
```

---

## 자체 검토

**스펙 커버리지**

| 스펙 18 항목 | 작업 |
|---|---|
| S5 화면 구성(제목, 회원증 카드, 안내, 로그아웃) | Task 3 Step 1 |
| 대체 표시(이름 없음, 사진 없음·실패, 이메일 없음) | Task 1(이름·이메일), Task 2(사진 실패) |
| 하단 탭 4칸, 로그아웃 폼·`.tabbar-signout` 제거, `/me` 경로 판정 | Task 3 Step 2~3 |
| `toProfile`/`loadProfile`, 다섯 키만 | Task 1 |
| img + no-referrer | Task 2 |
| 비로그인 처리(proxy + redirect) | Task 3 Step 1, Step 5-4 |
| 테스트 분기 5개 | Task 1 (6개. 스펙의 5개 + 메타데이터 email 폴백) |
| 문구 2개 | Task 1(`사용자`), Task 3(`Google 계정으로 로그인되어 있습니다`) |
| 관리자 버튼 유지(P6) | Task 3 Step 5-5 확인 |

**플레이스홀더**: 없음. **타입 일관성**: `Profile`의 `name/email/avatarUrl/initial`을 Task 3이 그대로 쓴다. `Avatar` props `src/initial/name`은 Task 2 정의와 Task 3 호출이 같다.
